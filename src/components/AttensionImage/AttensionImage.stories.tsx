import type { Meta } from '@storybook/react';
import AttensionImage from '.';

const meta: Meta<typeof AttensionImage> = {
    component: AttensionImage,
}

export default meta;

export const Default = {
    args: {
    },
};